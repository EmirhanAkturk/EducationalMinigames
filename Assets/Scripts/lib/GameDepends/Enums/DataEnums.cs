public enum CurrencyType
{
    Coin = 0, 
    Cosmetic = 1,
    Supply = 2,
}

public enum SettingType
{
    Haptic = 0,
    MuteAudio = 1,
}

public enum StateType
{
	Level = 0,
    MuteAudio = 1,
    ActiveLevel = 2,
    RateUsTime = 3,
}

public enum InventoryType
{
    ExpendTile = 0,
    Staff = 1,
    Upgrade = 2,
    TutorialStep = 3,
    SmartlookEvent = 4,
    OfflineData = 5,
}

public enum OfflineData
{
    CashRegister = 1,
    SupplyStation = 2,
}